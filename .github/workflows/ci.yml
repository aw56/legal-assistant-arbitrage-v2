---
# yamllint disable rule:truthy rule:line-length
name: ⚖️ CI/CD Pipeline — Legal Assistant Arbitrage v2.4

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}-backend
  IMAGE_TAG: ${{ github.ref_name }}

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: admin
          POSTGRES_DB: legal_assistant_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U admin -d legal_assistant_db"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: 🧹 Run pre-commit (lint)
        run: pre-commit run --all-files --show-diff-on-failure

      - name: 🧩 Lint Markdown (Strict)
        run: make lint-docs-strict

      - name: 🛠️ Fix Alembic heads if needed
        run: python3 scripts/fix_migrations.py

      - name: 🗄️ Run migrations
        env:
          DATABASE_URL: postgresql+psycopg2://admin:admin@localhost:5432/legal_assistant_db
        run: alembic upgrade head

      - name: ✅ Run tests
        env:
          DATABASE_URL: postgresql+psycopg2://admin:admin@localhost:5432/legal_assistant_db
        run: pytest -vv --disable-warnings

      - name: 📣 Notify Telegram (Success)
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: >
          make telegram-notify MESSAGE="✅ CI успешно завершён
          для Legal Assistant Arbitrage v2.4"

      - name: 📣 Notify Telegram (Failure)
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: >
          make telegram-notify MESSAGE="❌ Ошибка CI Legal Assistant Arbitrage v2.4 —
          проверь логи"

  smoke:
    runs-on: ubuntu-latest
    needs: lint-and-test

    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v4

      - name: 🐳 Build & run containers
        run: docker compose -f docker-compose.prod.yml up -d --build

      - name: ⏳ Wait for API
        run: |
          for i in {1..20}; do
            if curl -s http://127.0.0.1:8080/api/health | grep -q "ok"; then
              echo "✅ API is up"
              exit 0
            fi
            echo "⏳ Waiting for API..."
            sleep 5
          done
          echo "❌ API not responding" && exit 1

      - name: 🚦 Run smoke tests
        run: make smoke

      - name: 📣 Notify Telegram (Smoke Success)
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: >
          make telegram-notify MESSAGE="🌐 Smoke-тесты успешно пройдены:
          API отвечает на /health ✅"

      - name: 📣 Notify Telegram (Smoke Failure)
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: >
          make telegram-notify MESSAGE="🚨 Ошибка smoke-тестов:
          API не отвечает на /health ❌"
