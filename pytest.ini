[pytest]
# Папка, где хранятся тесты
testpaths = backend/app/tests

# Добавляем корень проекта в PYTHONPATH, чтобы импорты работали корректно
pythonpath = .

# Асинхронные тесты (FastAPI и asyncio поддержка)
asyncio_mode = auto

# 💡 Глобальные параметры вывода
addopts = -ra -q

# 🔖 Кастомные маркеры
markers =
    integration: тесты, взаимодействующие с внешними сервисами (например, Telegram API)
    smoke: быстрые проверки API /health и базовой функциональности
    slow: длительные тесты (например, парсинг или интеграции)

# ⚠️ Подавляем несущественные предупреждения от зависимостей (Python 3.12+)
filterwarnings =
    ignore:.*crypt.*:DeprecationWarning
    ignore:.*pkg_resources.*:DeprecationWarning
    ignore:.*distutils.*:DeprecationWarning
