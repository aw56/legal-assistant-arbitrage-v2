---
# ============================================
# ⚖️ Pre-commit DevOps Suite — Legal Assistant Arbitrage v2.8
# ✅ Stable Clean Edition (loop-free)
# ============================================

repos:
  # --- 🖤 Black: автоформатирование Python-кода ---
  - repo: https://github.com/psf/black
    rev: "25.9.0"
    hooks:
      - id: black
        name: black (formatter)
        language_version: python3.12
        args: [--line-length=88]
        exclude: ^node_modules/
        stages: [commit]

  # --- 🔤 isort: сортировка импортов ---
  - repo: https://github.com/PyCQA/isort
    rev: "6.1.0"
    hooks:
      - id: isort
        name: isort (imports)
        language_version: python3.12
        args: [--profile=black]
        exclude: ^node_modules/
        stages: [commit]

  # --- 🧹 flake8: статический анализ Python ---
  - repo: https://github.com/pycqa/flake8
    rev: "7.3.0"
    hooks:
      - id: flake8
        name: flake8 (project)
        language_version: python3.12
        entry: flake8 --config=.flake8
        types: [python]
        stages: [commit]
        exclude: >
          (?x)^(
            node_modules/|
            venv/|
            artifacts/|
            docs/|
            alembic/|
            backup/|
            build/|
            dist/
          )

  # --- 🧰 Базовые pre-commit проверки ---
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: "v6.0.0"
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: check-json
      - id: check-added-large-files
        exclude: ^node_modules/
        args: ["--maxkb=2000"]
      - id: check-merge-conflict
      # 🧹 Запуск только при commit
        stages: [commit]

  # --- 📝 MarkdownLint: форматирование документации ---
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: "v0.45.0"
    hooks:
      - id: markdownlint
        name: markdownlint (docs)
        entry: markdownlint
        language: node
        files: |
          ^docs/.*\.md$|
          ^README\.md$|
          ^CONTRIBUTING\.md$
        args: [--config, .markdownlint.yaml, --ignore-path, .markdownlintignore]
        stages: [commit]

  # --- 🧾 Yamllint: валидация YAML ---
  - repo: https://github.com/adrienverge/yamllint
    rev: "v1.37.1"
    hooks:
      - id: yamllint
        name: yamllint (project only)
        files: |
          ^\.github/workflows/.*\.ya?ml$|
          ^docker-compose.*\.ya?ml$|
          ^\.yamllint\.yml$
        exclude: |
          ^node_modules/|
          ^venv/|
          ^artifacts/|
          ^logs/
        args:
          - -d
          - "{extends: default, rules: {line-length: disable, truthy: {check-keys: false}}}"
        stages: [commit]

  # --- 🔒 Detect Secrets: поиск токенов и паролей ---
  - repo: https://github.com/Yelp/detect-secrets
    rev: "v1.5.0"
    hooks:
      - id: detect-secrets
        args: [--baseline, .secrets.baseline]
        exclude: |
          ^tests/|
          ^artifacts/|
          ^docs/|
          ^node_modules/|
          ^backup/
        stages: [commit]

  # --- 🧪 Pytest перед пушем (pre-push) ---
  - repo: local
    hooks:
      - id: run-pytest-before-push
        name: Run pytest before push
        entry: bash -c 'pytest --maxfail=1 --disable-warnings -q || exit 1'
        language: system
        stages: [pre-push]
        pass_filenames: false

  # --- 🚫 Отключённый автокоммит после фиксов ---
  # Причина: вызывал бесконечный цикл amend → push rollback.
  # Можно включить вручную позже при полной стабилизации.
  # - repo: local
  #   hooks:
  #     - id: auto-commit-after-fix
  #       name: Auto commit after pre-commit fixes
  #       entry: |
  #         bash -c '
  #         if git diff --quiet; then
  #           echo "[auto-commit-after-fix] Nothing to fix, skipping.";
  #         else
  #           echo "[auto-commit-after-fix] Detected fixed files.";
  #           git add -u &&
  #           git commit --amend --no-edit || true;
  #         fi
  #         '
  #       language: system
  #       pass_filenames: false
  #       stages: [manual]
