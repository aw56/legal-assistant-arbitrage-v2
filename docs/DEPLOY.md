# 🚀 DEPLOY — Legal Assistant API v2

Руководство по развёртыванию проекта в продакшене.

---

## 🔧 1. Подготовка сервера

Установить зависимости:
```bash
sudo apt update
sudo apt install -y curl git make python3-venv python3-pip
