Прекрасно 💼 — вот тебе **финальная, полностью оформленная шпаргалка**.
Она объединяет все шаги: генерацию коллекции через `make`, скачивание с Linux на Windows, импорт в Postman и PowerShell-автоматизацию.

Готов к копированию в `docs/Import_Postman_Windows.md` 👇

---

# ⚙️ Шпаргалка: как сгенерировать и импортировать Postman-коллекцию с Linux-сервера на Windows

---

## 🎯 Цель

Передать свежую Postman-коллекцию (`postman_collection.zip`)
из проекта **Legal Assistant Arbitrage v2** на Windows-компьютер
и запустить автотесты в Postman одним кликом.

---

## 🧩 1. Генерация коллекции на сервере

На Linux-сервере, из каталога проекта:

```
cd ~/my_projects/legal-assistant-arbitrage-v2
```

Выполни команду:

```bash
make postman HOST_URL=http://82.165.144.150:8080
```

### 📘 Что делает команда

| Этап           | Действие                                                                                                          |
| -------------- | ----------------------------------------------------------------------------------------------------------------- |
| `make postman` | вызывает Python-скрипт `scripts/generate_postman.py`, который генерирует коллекцию с учётом указанного `HOST_URL` |
| `HOST_URL`     | задаёт публичный адрес API (в проде — IP сервера или домен)                                                       |
| Результат      | создаётся файл `docs/postman_collection.json`                                                                     |
| Автоматически  | далее вызывается цель `make postman-export`, упаковывая коллекцию в `artifacts/postman_collection.zip`            |

📍После успешного выполнения появится сообщение:

```
✅ Архив сохранён: artifacts/postman_collection.zip
```

---

## 🔐 2. Проверка SSH-доступа

Первое подключение:

```
The authenticity of host '82.165.144.150' can't be established.
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

Введи:

```
yes
```

Если SSH жалуется:

```
WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!
```

— просто сбрось старый ключ:

```powershell
ssh-keygen -R 82.165.144.150
```

---

## 💾 3. Скачивание файла на Windows через PowerShell

Открой PowerShell и выполни:

```powershell
scp admin@82.165.144.150:/home/admin/my_projects/legal-assistant-arbitrage-v2/artifacts/postman_collection.zip C:\Users\Aleksej\Downloads\postman_collection.zip
```

> ⚠️ Убедись, что указал **имя файла после `Downloads\`**.
> Если хочешь сохранить в другое место, просто измени путь, например:
>
> ```powershell
> scp admin@82.165.144.150:/home/admin/my_projects/legal-assistant-arbitrage-v2/artifacts/postman_collection.zip C:\postman_collection.zip
> ```

---

## 🌐 4. Альтернатива: HTTP-загрузка без SCP

На сервере:

```bash
make postman-serve
```

➡️ После этого в браузере Windows открой:

```
http://82.165.144.150:8080/postman_collection.zip
```

Скачаешь архив прямо через HTTP.
После загрузки нажми **Enter** в терминале, чтобы остановить сервер.

---

## 📦 5. Импорт коллекции в Postman (на Windows)

1. Открой **Postman**
2. Нажми **Import → File**
3. Выбери `postman_collection.zip`
4. После импорта выбери окружение (Environment)
5. Введи переменную:

   ```
   base_url = http://82.165.144.150:8080
   ```
6. Нажми **Runner → Run Collection**

✅ Коллекция отработает полностью (Auth / Laws / Decisions / Users / Health).

---

## ⚙️ 6. (Опционально) Автоматизация через PowerShell-скрипт

Создай файл `C:\Users\Aleksej\Scripts\download_postman.ps1`:

```powershell
# ===========================
# 🚀 Auto Download Postman Kit
# ===========================

$server = "admin@82.165.144.150"
$remotePath = "/home/admin/my_projects/legal-assistant-arbitrage-v2/artifacts/postman_collection.zip"
$localPath = "$env:USERPROFILE\Downloads\postman_collection.zip"

Write-Host "🧩 Очистка старых SSH ключей..."
ssh-keygen -R 82.165.144.150 | Out-Null

Write-Host "📥 Скачиваю Postman коллекцию..."
scp "$server:$remotePath" "$localPath"

if (Test-Path $localPath) {
    Write-Host "✅ Файл скачан: $localPath"
    Write-Host "📂 Открой Postman → Import → File и выбери postman_collection.zip"
} else {
    Write-Host "❌ Ошибка: файл не найден после скачивания."
}
```

Запуск:

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\Aleksej\Scripts\download_postman.ps1"
```

---

## 💡 7. Полный цикл Makefile + Windows-импорт

| Этап | Команда                                                                                                                                                            | Результат                      |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------ |
| 1    | `make postman HOST_URL=http://82.165.144.150:8080`                                                                                                                 | генерация и упаковка коллекции |
| 2    | `scp admin@82.165.144.150:/home/admin/my_projects/legal-assistant-arbitrage-v2/artifacts/postman_collection.zip C:\Users\Aleksej\Downloads\postman_collection.zip` | загрузка на Windows            |
| 3    | Импорт в Postman → Run Collection                                                                                                                                  | проверка API                   |

---

## 🧠 Итого

| Цель                     | Команда                                                                                                                                                            |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Сгенерировать коллекцию  | `make postman HOST_URL=http://82.165.144.150:8080`                                                                                                                 |
| Сбросить SSH-ключ        | `ssh-keygen -R 82.165.144.150`                                                                                                                                     |
| Скачать через PowerShell | `scp admin@82.165.144.150:/home/admin/my_projects/legal-assistant-arbitrage-v2/artifacts/postman_collection.zip C:\Users\Aleksej\Downloads\postman_collection.zip` |
| Альтернатива (HTTP)      | `make postman-serve`                                                                                                                                               |
| Импортировать в Postman  | **Import → File → Run Collection**                                                                                                                                 |

---

🧾 **Рекомендация:**
Сохрани этот документ в проекте:

```
/home/admin/my_projects/legal-assistant-arbitrage-v2/docs/Import_Postman_Windows.md
```

и добавь его в Git — это отличный DevOps-гайд для новых разработчиков и QA.
